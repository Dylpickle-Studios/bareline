# Repository Word Count example

This standalone example demonstrates a repository tab, command, schema-rendered settings,
repository content access, logging, and namespaced storage. It is not bundled as an enabled
application plugin.

The manifest marks the JavaScript runtime as trusted. Installing and enabling it is equivalent to
installing server software. Review its source before accepting that risk. For hostile or unknown
code, use a sandboxed WebAssembly plugin instead.
